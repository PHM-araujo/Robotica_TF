% Parametros Robô
l0 = 2;
l1 = 2;
l2 = 2;
l3 = 2;

% Cinematica direta
%L = Link([theta d a alpha]);
L1=Link([0 0 0 0]);
L2=Link([0 0 l0 pi/2]);
L3=Link([0 0 l1 0]);
L4=Link([0 0 l2 0]);
L5=Link([0 0 l3 0]);

Rbt=SerialLink([L1 L2 L3 L4 L5]);
q = [pi/4 0 pi/4 0 0];

% Cinematica inversa
% Gerar matriz de posição da ferramenta
pos_


%Rbt.plot(q)









